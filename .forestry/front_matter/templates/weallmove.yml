---
label: WeAllMove
hide_body: true
fields:
- type: field_group_list
  name: operators
  label: Operators
  fields:
  - type: text
    name: name
    label: Name
  - type: text
    name: url
    label: URL
  - type: file
    name: logo
    label: Logo
  config:
    labelField: title
- type: list
  name: hero_logo_names
  label: hero_logo_names
- name: all_services
  type: field_group_list
  fields:
  - name: service_name
    type: text
    config:
      required: false
    label: Service Name
    description: Few words to describe this offering
  - name: service_description
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Service Description
    description: More detailed description of service for the front-end. Use {company_name}
      to include it in the description
  - name: need_this_helps_with
    type: select
    default: ''
    config:
      required: false
      options: []
      source:
        type: documents
        section: weallmove
        file: _data/weallmove.yml
        path: all_needs
    label: Need this helps with
  config:
    min: 
    max: 
    labelField: 
  label: All Services
  description: List all the possible services operators can offer
- name: all_needs
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: All Needs
pages:
- _data/weallmove.yml
