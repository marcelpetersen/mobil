---
label: WeAllMove
hide_body: true
fields:
- name: operators
  type: field_group_list
  config:
    min: 
    max: 
    labelField: 
  label: Operators
- name: all_countries
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: All Countries
- name: all_cities
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: All Cities
