---
label: Speakers
hide_body: true
fields:
- name: heading
  type: text
  config:
    required: false
  label: Heading
  default: Speaker Line-up
- name: sub-heading
  type: text
  config:
    required: false
  label: Sub-heading
- name: speakers
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: Name
  - name: job_title
    type: text
    config:
      required: false
    label: Job Title
  - name: company
    type: text
    config:
      required: false
    label: Company
  - name: speaker_photo
    type: file
    config:
      maxSize: 250
    label: Speaker Photo
  - name: company_logo
    type: file
    config:
      maxSize: 250
    label: Company Logo
  config:
    min: 
    max: 
    labelField: 
  label: Speakers
- name: block
  type: text
  config:
    required: false
  label: block
  hidden: true
  default: speaker-cards
