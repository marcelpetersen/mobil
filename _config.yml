title: Wunder Mobility
author: Ben Kammerling
email: website@wundermobility.com
description: >-
  A smart mobility marketplace and tech platform providing carpooling, smart shuttles, and scooter/car-sharing to end customers, corporates, and cities.
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://wundermobility.com" # your site's origin, e.g. https://forestry.io

collections:
  - articles

include: ['_redirects']

# Build settings
defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default" # this ensures all content renders with layouts/default.html
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-menus
sass:
  sass_dir: _sass

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
